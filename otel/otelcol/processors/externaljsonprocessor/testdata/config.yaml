receivers:
  examplereceiver:

processors:
  externaljson:

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers:
      - examplereceiver
      processors:
      - externaljson
      exporters:
      - exampleexporter